.row.collapse
  .right= link_to fa_icon('plus', text: 'Add'), new_event_path, class: 'button-xs add-btn', remote: true
  .large-6.medium-8.small-8.columns
    %h2 Events
.row
  = react_component('FilterBar', resource_url: events_path)

.row
  #event-list
    = render 'events'

:javascript
   window.filters = [
      { field: "name_cont", uid: "name_cont", type: "search", input: 'text', placeholder: 'Name' }
    ];
