%h1 Listing teams

%table
  %tr
    %th Participant
    %th Event
    %th Team name
    %th
    %th
    %th

  - @teams.each do |team|
    %tr
      %td= team.participant_id
      %td= team.event_id
      %td= team.team_name
      %td= link_to 'Show', team
      %td= link_to 'Edit', edit_team_path(team)
      %td= link_to 'Delete', team, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Team', new_team_path
