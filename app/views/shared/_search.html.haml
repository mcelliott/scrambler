.panel{ style: 'padding-bottom: 0;' }
  .row.collapse
    = search_form_for @q, url: path, remote: true do |f|
      - filters.each do |filter|
        .div{ class: "#{filter[:width]} columns" }
          = f.search_field filter[:field], placeholder: 'Search Name or Location', class: filter[:class]
      .small-4.columns
        .button-bar
          %ul.button-group
            %li= f.submit 'Search', class: 'postfix button-xs search-btn'
            %li= link_to 'Clear', nil, class: 'postfix button-xs search-btn'

:coffeescript
  $("input.datepicker").datepicker({ dateFormat: "dd/mm/yy", firstDay: "1", changeMonth: true, changeYear: true })
