%h2.text-center Score Handicapping

.row
  .large-12.medium-12.small-12.columns.small-centered
    #handicap-list
      %table
        %thead
          %tr
            %th Title
            %th Tunnel Hours
            %th Handicap Amount
            %th
        %tbody#handicaps
          - @handicaps.each do |handicap|
            %tr{ id: "handicap-#{handicap.id}" }
              = render handicap
