.row.collapse
  .right= link_to fa_icon('plus', text: 'Add'), new_page_path, class: 'button-xs add-btn', remote: true
  .large-6.medium-8.small-8.columns
    %h2 Pages

.row
  = react_component('FilterBar', resource_url: pages_path)

.row
  #page-list= render 'pages'

:javascript
   window.filters = [
      { field: "name_or_title_or_slug_cont", uid: "name_cont", type: "search", input: 'text',   placeholder: 'Name, Title, Slug' }
    ];

