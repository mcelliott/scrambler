= simple_form_for @page, remote: true do |f|
  = f.error_notification
  = f.input :name
  = f.input :title
  = f.input :content, as: :text, input_html: { rows: 40 }
  = f.button :submit, class: 'button-xs radius'
