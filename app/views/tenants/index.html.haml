.row.collapse
  .right= link_to fa_icon('plus', text: 'Add'), new_tenant_path, class: 'button-xs add-btn', remote: true
  .large-6.medium-8.small-8.columns
    %h2 Tenants

.row
  = react_component('FilterBar', resource_url: tenants_path)

.row
  #tenant-list= render 'tenants'

:javascript
   window.filters = [
      { field: "name_or_database_or_domain_cont", uid: "name_or_database_or_domain_cont", type: "search", input: 'text',   placeholder: 'Name, Domain, Database' }
    ];

