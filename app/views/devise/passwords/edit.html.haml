%h3.text-center Change your password
.row
  .large-4.small-6.columns.small-centered.underline
    = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f|
      = f.input :reset_password_token, as: :hidden
      = f.input :password, label: false, placeholder: 'New password', required: true, autofocus: true
      = f.input :password_confirmation, label: false, placeholder: 'Confirm your new password', required: true
      = f.button :submit, 'Change my password', class: 'button-xs devise-button'
.row
  .large-4.small-6.columns.small-centered.text-center
    = link_to 'Resend instructions', new_confirmation_path(resource_name)
